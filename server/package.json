{
  "name": "minecraft-map",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cli": "ts-node ./src/main.ts",
    "serve:dev": "npm run build && node ./server.js",
    "build:client": "rm -rf ./client && cp ../client/public ./client && esbuild --bundle --outfile=./client/index.js ../client/index.ts",
    "build": "esbuild --bundle --platform=node --outfile=./server.js src/server.ts && pkg ./server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/pngjs": "^6.0.4",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.19.10"
  },
  "dependencies": {
    "@fastify/send": "^2.1.0",
    "@fastify/static": "^6.12.0",
    "@fastify/websocket": "^8.3.0",
    "@types/adm-zip": "^0.5.5",
    "@zip.js/zip.js": "^2.7.32",
    "adm-zip": "^0.5.10",
    "fastify": "^4.25.2",
    "pkg": "^5.8.1",
    "pngjs": "^7.0.0",
    "ts-node": "^10.9.2"
  }
}
